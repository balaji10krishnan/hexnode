import classes from "./MessageTap.module.css";
const MessageTap = () => {
  return (
    <div className={classes["message-con"]}>
      <div className={classes["title-con"]}>
        <p>Messages</p>
      </div>
    </div>
  );
};

export default MessageTap;
